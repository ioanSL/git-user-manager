[package]
name = "git-user-manager"
version = "1.0.0"
edition = "2021"
description = "Manage multiple git identities (GitHub/GitLab users) with auto-switching by remote"
license = "MIT"

[[bin]]
name = "gum"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
dirs = "5"
anyhow = "1"
ratatui = "0.29"

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
tempfile = "3"

[profile.release]
strip = true
lto = true

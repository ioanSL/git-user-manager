# Cross-link the aarch64 musl target with the bundled LLVM linker (rust-lld),
# so `cargo build --target aarch64-unknown-linux-musl` needs no external
# cross-gcc. Works because the crate tree is pure Rust (no C dependencies).
[target.aarch64-unknown-linux-musl]
linker = "rust-lld"
